
const intialState = {
  updateCollection: false
}

export default function modalReducer(state=intialState, action){
  switch(action.type){

  case "MODAL_TOGGLE":

    default:
      return state;
  }
}
